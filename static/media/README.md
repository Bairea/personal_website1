## static/media 图床目录

- 该目录用于存放文章引用的图片资源，构建后会原样发布到 `public/media/**`
- 推荐路径：`static/media/<yyyy>/<mm>/<slug>.<ext>`
- 文章中统一使用绝对路径引用：`/media/<yyyy>/<mm>/<slug>.<ext>`

示例：

```md
![示例图](/media/2026/03/discovery-pipeline.webp)
```
