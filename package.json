{
  "name": "personal-website",
  "private": true,
  "scripts": {
    "build": "hugo --cleanDestinationDir && npx pagefind --site public --force-language zh",
    "dev": "hugo server --buildDrafts --bind 127.0.0.1"
  },
  "devDependencies": {
    "hugo-bin": "^0.149.2",
    "pagefind": "^1.5.2"
  }
}