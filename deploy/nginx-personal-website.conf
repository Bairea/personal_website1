# Nginx configuration for personal-website
# Replace YOUR_DOMAIN with your actual domain name.
# Replace /srv/personal-website with the actual deployment path.
#
# Security response headers live in snippets/security-headers.conf and are
# included by every location below. Deploy that snippet to /etc/nginx/snippets/
# (see docs/DEPLOY.md). Keep in sync with server/index.js (local preview).

# Rate limiting zone for API endpoints
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/m;

# HTTP -> HTTPS redirect (enable after domain + HTTPS is set up)
# server {
#     listen 80;
#     server_name YOUR_DOMAIN www.YOUR_DOMAIN;
#
#     # Let's Encrypt challenge path (needed before HTTPS is set up)
#     location /.well-known/acme-challenge/ {
#         root /var/www/html;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# HTTP-only server (use before domain/HTTPS is available)
server {
    listen 80;
    server_name YOUR_DOMAIN;

    # Static files served directly by Nginx (better performance than Express)
    root /srv/personal-website/public;
    index index.html;

    # Cache static assets
    location ~* \.(css|js|json|woff2?|png|jpg|jpeg|gif|ico|svg|webp|wasm)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        include snippets/security-headers.conf;
        try_files $uri =404;
    }

    # HTML pages - short cache for content freshness
    location ~* \.html$ {
        expires 1h;
        add_header Cache-Control "public";
        include snippets/security-headers.conf;
        try_files $uri =404;
    }

    # Default: try static file first
    location / {
        include snippets/security-headers.conf;
        try_files $uri $uri/ =404;
    }

    # API requests proxied to Express on loopback
    location /api/ {
        include snippets/security-headers.conf;
        limit_req zone=api burst=10 nodelay;
        limit_req_status 429;

        proxy_pass http://127.0.0.1:8787;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Disable caching for API responses
        add_header Cache-Control "no-store" always;
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
        return 404;
    }
}

# HTTPS server (uncomment and configure after domain + certbot is set up)
# server {
#     listen 443 ssl http2;
#     server_name YOUR_DOMAIN www.YOUR_DOMAIN;
#
#     # TLS certificates (managed by Certbot)
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#
#     # TLS hardening
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_session_tickets off;
#
#     # HSTS - only enable after HTTPS is confirmed working
#     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
#
#     # Static files served directly by Nginx
#     root /srv/personal-website/public;
#     index index.html;
#
#     # Cache static assets
#     location ~* \.(css|js|json|woff2?|png|jpg|jpeg|gif|ico|svg|webp|wasm)$ {
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#         include snippets/security-headers.conf;
#         try_files $uri =404;
#     }
#
#     # HTML pages
#     location ~* \.html$ {
#         expires 1h;
#         add_header Cache-Control "public";
#         include snippets/security-headers.conf;
#         try_files $uri =404;
#     }
#
#     # Default
#     location / {
#         include snippets/security-headers.conf;
#         try_files $uri $uri/ =404;
#     }
#
#     # API requests proxied to Express on loopback
#     location /api/ {
#         include snippets/security-headers.conf;
#         limit_req zone=api burst=10 nodelay;
#         limit_req_status 429;
#
#         proxy_pass http://127.0.0.1:8787;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         add_header Cache-Control "no-store" always;
#     }
#
#     # Deny access to hidden files
#     location ~ /\. {
#         deny all;
#         return 404;
#     }
# }
